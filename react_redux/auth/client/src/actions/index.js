export function signinUser({ email, password }) {
  // submti email/pw to server


  // if req good
  // update state to indicate user is authenticated
  // save jwt token
  // redirect to route /feature

  // if req bad
  // show error to user
}